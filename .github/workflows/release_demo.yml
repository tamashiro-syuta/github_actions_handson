name: release demo

on:
    push:
      branches:
        - "master"
      tags:
        - "v[0-9]+.[0-9]+.[0-9]+-dev"

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"
